# Backend FastAPI — wrappe les scripts d'expérience en flux SSE pour le wizard UI.
# Installer dans le venv du projet :  pip install -r requirements.txt
fastapi==0.136.3
uvicorn==0.48.0
sse-starlette==3.4.4
numpy>=2.0
